# Base URL of the WriterApp app used by CTA links.
# Local default (when omitted during `next dev`): http://localhost:5387
# Production default (when omitted in non-dev): Azure production URL.
NEXT_PUBLIC_APP_URL=https://prosa-fgg2cxhbdja2hwee.swedencentral-01.azurewebsites.net
